/// Defines the available layout types for the ImageGrid widget.
enum ImageGridType {
  /// Traditional 2x2 grid with 3 images below
  classic,

  /// Four vertical columns with alternating heights
  columns,

  /// Large banner image with text and smaller images below
  banner,

  /// Two large images on left, three smaller images on right
  frame,
}
